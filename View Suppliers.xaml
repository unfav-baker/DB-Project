<Page x:Class="Employee.View_Suppliers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Employee"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="View Suppliers and Their Orders" Background="#FFF0F2F5">
    <Page.Resources>
        <Style x:Key="SupplierCardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Width" Value="280"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" Opacity="0.1" ShadowDepth="1" Color="#FFBBBBBB"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SelectedSupplierCardBorderStyle" TargetType="Border" BasedOn="{StaticResource SupplierCardBorderStyle}">
            <Setter Property="BorderBrush" Value="#FF065668"/>
            <Setter Property="BorderThickness" Value="2.5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="12" Opacity="0.3" ShadowDepth="3" Color="#FF065668"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SupplierTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FF065668"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style x:Key="SupplierDetailLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,3,5,3"/>
        </Style>
        <Style x:Key="SupplierDetailValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,3,0,3"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style x:Key="RightAlignedTextCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="61"/>
            <RowDefinition Height="Auto" MinHeight="200"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF065668">
            <TextBlock Text="Suppliers &amp; Orders" HorizontalAlignment="Left" VerticalAlignment="Center" 
                       Margin="20,0,0,0" Foreground="White" FontSize="22" FontWeight="Bold"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="220" Padding="5,0,5,5">
            <ItemsControl ItemsSource="{Binding SuppliersList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:SupplierDisplayData}">
                        <Button Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand"
                                Click="SupplierCard_Click" CommandParameter="{Binding}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                            <Border x:Name="SupplierCardBorder" Style="{StaticResource SupplierCardBorderStyle}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource SupplierTitleStyle}"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Phone:" Style="{StaticResource SupplierDetailLabelStyle}"/>
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding PhoneNumber}" Style="{StaticResource SupplierDetailValueStyle}"/>

                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" Style="{StaticResource SupplierDetailLabelStyle}"/>
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Email}" Style="{StaticResource SupplierDetailValueStyle}"/>

                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Plant:" Style="{StaticResource SupplierDetailLabelStyle}"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PlantName}" Style="{StaticResource SupplierDetailValueStyle}"/>

                                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Status:" Style="{StaticResource SupplierDetailLabelStyle}"/>
                                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Status}" Style="{StaticResource SupplierDetailValueStyle}"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </Button>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSelectedAsCurrent}" Value="True">
                                <Setter TargetName="SupplierCardBorder" Property="Style" Value="{StaticResource SelectedSupplierCardBorderStyle}"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Rectangle Grid.Row="2" Fill="#DDDDDD" Height="1" Margin="10,0"/>

        <Grid Grid.Row="3" Margin="10,5,10,10">
            <TextBlock Text="Orders for Selected Supplier:" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#333"/>
            <DataGrid x:Name="OrdersDataGrid" ItemsSource="{Binding SelectedSupplierOrders}" 
                      AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,30,0,0"
                      HeadersVisibility="Column" IsReadOnly="True" SelectionMode="Single" 
                      BorderBrush="#CCCCCC" BorderThickness="1"
                      HorizontalGridLinesBrush="#FFE0E0E0" VerticalGridLinesBrush="#FFE0E0E0"
                      RowBackground="White" AlternatingRowBackground="#FFF9F9F9"
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <DataGrid.Style>
                    <Style TargetType="DataGrid">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedSupplier}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Style>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order ID" Binding="{Binding OrderId}" MinWidth="70" Width="Auto" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Order Date" Binding="{Binding OrderDate, StringFormat='dd/MM/yyyy'}" MinWidth="90" Width="Auto"/>
                    <DataGridTextColumn Header="Party Name" Binding="{Binding PartyName}" MinWidth="150" Width="2*"/>
                    <DataGridTextColumn Header="Phyto Num" Binding="{Binding PhytoNumber}" MinWidth="100" Width="*"/>
                    <DataGridTextColumn Header="Cartons" Binding="{Binding Carton}" MinWidth="60" Width="Auto" ElementStyle="{StaticResource RightAlignedTextCellStyle}"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding TotalAmount, StringFormat=C}" MinWidth="90" Width="*" ElementStyle="{StaticResource RightAlignedTextCellStyle}"/>
                    <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat=C}" MinWidth="90" Width="*" ElementStyle="{StaticResource RightAlignedTextCellStyle}"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock Text="Select a supplier above to view their orders." FontSize="14" Foreground="Gray" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,30,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedSupplier}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Grid>
</Page>
