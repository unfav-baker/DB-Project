<Page x:Class="Employee.EditProfile"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Employee"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="800" Title="Edit Employee Profile" Background="#FFF0F2F5">
    <Page.Resources>
        <Style TargetType="TextBlock" x:Key="FormLabelStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FF065668"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBox" x:Key="FormInputStyle">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF065668"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFEFEFEF"/>
                                <Setter Property="Foreground" Value="#FF555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DatePicker" x:Key="FormDatePickerStyle">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="FormComboBoxStyle">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="CheckBox" x:Key="FormCheckBoxStyle">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="Foreground" Value="#FF065668"/>
        </Style>

        <Style TargetType="Button" x:Key="HeaderCloseButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <TextBlock Text="✕" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="FormSaveButtonStyle">
            <Setter Property="Background" Value="#FF065668"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF065668"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF043A4A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FFB0B0B0"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF909090"/>
                                <Setter Property="Foreground" Value="#FF606060"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="FormClearButtonStyle">
            <Setter Property="Background" Value="#FFE0E0E0"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFD0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FFB0B0B0"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF909090"/>
                                <Setter Property="Foreground" Value="#FF606060"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="61"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF065668">
            <Grid>
                <TextBlock Text="✏️ Edit Employee Profile" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="24,0,0,0" Foreground="White" FontSize="22" FontWeight="Bold"/>
                <Button Content="✕" Click="CancelButton_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource HeaderCloseButtonStyle}" ToolTip="Close and Discard Changes"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="20">
            <Border Background="White" CornerRadius="6" Padding="25" MaxWidth="750" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="#DDDDDD" ShadowDepth="1" BlurRadius="10" Opacity="0.4"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Edit Information" FontSize="20" FontWeight="SemiBold" Foreground="#FF333333" Margin="0,0,0,25" HorizontalAlignment="Left"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Define Rows as needed for your content. 
                             Using Auto makes them size to content. 
                             If you have many rows, consider if this Grid is the best approach or if separate StackPanels/Borders are better.
                             For simplicity, I'm keeping your structure but ensure it flows well. -->
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,15">
                            <TextBlock Text="Full Name *" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox x:Name="FullNameTextBox" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormInputStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,15">
                            <TextBlock Text="Gender" Style="{StaticResource FormLabelStyle}"/>
                            <ComboBox x:Name="GenderComboBox" SelectedValue="{Binding Gender}" SelectedValuePath="Content" Style="{StaticResource FormComboBoxStyle}">
                                <ComboBoxItem Content="Male"/>
                                <ComboBoxItem Content="Female"/>
                                <ComboBoxItem Content="Other"/>
                                <ComboBoxItem Content="Prefer not to say"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,15">
                            <TextBlock Text="Date of Birth" Style="{StaticResource FormLabelStyle}"/>
                            <DatePicker x:Name="DateOfBirthPicker" SelectedDate="{Binding DateOfBirth, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormDatePickerStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,15">
                            <TextBlock Text="Address" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox x:Name="AddressTextBox" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormInputStyle}" AcceptsReturn="True" TextWrapping="Wrap" Height="76"/>
                        </StackPanel>
                        <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,0,15">
                            <TextBlock Text="Language Preference" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox x:Name="LanguagePreferenceTextBox" Text="{Binding LanguagePreference, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormInputStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="5" Grid.Column="0" Margin="0,0,0,15">
                            <TextBlock Text="Employee ID (Read-only)" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox Text="{Binding EmployeeId}" Style="{StaticResource FormInputStyle}" IsReadOnly="True"/>
                        </StackPanel>
                        <StackPanel Grid.Row="6" Grid.Column="0" Margin="0,0,0,15">
                            <TextBlock Text="Role (Read-only)" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox Text="{Binding Role}" Style="{StaticResource FormInputStyle}" IsReadOnly="True"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,15">
                            <TextBlock Text="Email Address *" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox x:Name="EmailTextBox" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormInputStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,15">
                            <TextBlock Text="Phone Number" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox x:Name="PhoneNumberTextBox" Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormInputStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,15">
                            <TextBlock Text="Nationality" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox x:Name="NationalityTextBox" Text="{Binding Nationality, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormInputStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,0,0,15">
                            <TextBlock Text="Time Zone" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox x:Name="TimeZoneTextBox" Text="{Binding TimeZone, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormInputStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="4" Grid.Column="2" Margin="0,0,0,15">
                            <TextBlock Text="Enable Login Notifications" Style="{StaticResource FormLabelStyle}"/>
                            <CheckBox x:Name="LoginNotificationsCheckBox" IsChecked="{Binding LoginNotificationsEnabled}" Style="{StaticResource FormCheckBoxStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="5" Grid.Column="2" Margin="0,0,0,15">
                            <TextBlock Text="Hire Date (Read-only)" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox Text="{Binding HireDate, StringFormat='dd MMMM yyyy'}" Style="{StaticResource FormInputStyle}" IsReadOnly="True"/>
                        </StackPanel>
                        <StackPanel Grid.Row="6" Grid.Column="2" Margin="0,0,0,15">
                            <TextBlock Text="Last Login (Read-only)" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox Text="{Binding LastLogin, StringFormat='dd MMM yyyy, hh:mm tt'}" Style="{StaticResource FormInputStyle}" IsReadOnly="True"/>
                        </StackPanel>
                    </Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
                        <Button Content="Cancel" Click="CancelButton_Click" Width="120" Height="38" Margin="0,0,10,0" Style="{StaticResource FormClearButtonStyle}"/>
                        <Button x:Name="SaveChangesButton" Content="Save Changes" Click="SaveButton_Click" Width="150" Height="38" Style="{StaticResource FormSaveButtonStyle}"/>
                    </StackPanel>

                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Page>
