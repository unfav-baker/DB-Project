# MySQL Database Connection String
# ---------------------------------
# Copy this file to a new file named .env (in the same directory)
# and replace the placeholder values with your actual local database credentials.
# The .env file is (and should be) ignored by Git.

PRIMETECH_DB_CONN_STRING="Server=127.0.0.1;Port=3306;Database=ems_project;Uid=root;Pwd=VORTEX@20000;"