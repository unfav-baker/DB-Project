<Page x:Class="Employee.View_Customer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="View Suppliers"
      Background="#F8F8F8">

    <Page.Resources>
        <Style x:Key="SupplierCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="20"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Height" Value="200"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SupplierTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FF065668"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="AddButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF065668"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="20"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0A768C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Supplier Cards -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Top">
                <!-- Supplier 1 Card -->
                <Border Style="{StaticResource SupplierCardStyle}">
                    <StackPanel>
                        <TextBlock Text=" Cutomer 1" Style="{StaticResource SupplierTitleStyle}"/>
                        <TextBlock Text="Data will be added here" 
                                 Foreground="#A0A0A0"
                                 FontStyle="Italic"/>
                    </StackPanel>
                </Border>

                <!-- Supplier 2 Card -->
                <Border Style="{StaticResource SupplierCardStyle}">
                    <StackPanel>
                        <TextBlock Text=" Customer 2" Style="{StaticResource SupplierTitleStyle}"/>
                        <TextBlock Text="Data will be added here" 
                                 Foreground="#A0A0A0"
                                 FontStyle="Italic"/>
                    </StackPanel>
                </Border>

                <!-- Supplier 3 Card -->
                <Border Style="{StaticResource SupplierCardStyle}">
                    <StackPanel>
                        <TextBlock Text=" Customer 3" Style="{StaticResource SupplierTitleStyle}"/>
                        <TextBlock Text="Data will be added here" 
                                 Foreground="#A0A0A0"
                                 FontStyle="Italic"/>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </ScrollViewer>

        <!-- Add New Supplier Button -->
        <Grid Height="45" VerticalAlignment="Bottom" Background="White">
            <Button Content="Add New Customer" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.718,-0.081" Background="#FF065668" BorderBrush="#FF065668" Width="118" Foreground="White" Height="27"/>
        </Grid>

    </Grid>
</Page>