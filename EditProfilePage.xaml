<Page x:Class="Adminn.EditProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Adminn"
      mc:Ignorable="d"
      d:DesignHeight="720" d:DesignWidth="800"
      Title="Edit Admin Profile">
    <Page.Resources>
        <Style TargetType="TextBlock" x:Key="EditLabelStyle">
            <Setter Property="Margin" Value="0,5,10,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
        <Style TargetType="TextBox" x:Key="EditTextBoxStyle">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="280"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF065668"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DatePicker" x:Key="EditDatePickerStyle">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="280"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="EditComboBoxStyle">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="280"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButtonStyle" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FF065668"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="MinWidth" Value="130"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF043A4A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="SecondaryButtonStyle" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FFE0E0E0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFBDBDBD"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,5,10,5"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFBDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="20" Background="WhiteSmoke">
        <Border MaxWidth="700" HorizontalAlignment="Center" Background="White" Padding="25" CornerRadius="8" Margin="0,20">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="4" Direction="320" Color="Black" Opacity="0.2" BlurRadius="10"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Edit Profile Information" FontSize="24" FontWeight="Bold" Margin="0,0,0,25" Grid.Row="0" Foreground="#FF065668"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Full Name:" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EditTextBoxStyle}" x:Name="FullNameEditTextBox"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Display Name:" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EditTextBoxStyle}" x:Name="DisplayNameEditTextBox"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Date of Birth:" Style="{StaticResource EditLabelStyle}"/>
                    <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding DateOfBirth, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EditDatePickerStyle}" x:Name="DobEditDatePicker"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Gender:" Style="{StaticResource EditLabelStyle}"/>
                    <ComboBox Grid.Row="3" Grid.Column="1" SelectedValue="{Binding Gender, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" Style="{StaticResource EditComboBoxStyle}" x:Name="GenderEditComboBox">
                        <ComboBoxItem Content="Male"/>
                        <ComboBoxItem Content="Female"/>
                        <ComboBoxItem Content="Other"/>
                        <ComboBoxItem Content="Prefer not to say"/>
                    </ComboBox>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Nationality:" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Nationality, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EditTextBoxStyle}" x:Name="NationalityEditTextBox"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Address:" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EditTextBoxStyle}" TextWrapping="Wrap" AcceptsReturn="True" Height="70" VerticalScrollBarVisibility="Auto" x:Name="AddressEditTextBox"/>

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Phone Number:" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EditTextBoxStyle}" x:Name="PhoneNumberEditTextBox"/>

                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Email:" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EditTextBoxStyle}" x:Name="EmailEditTextBox"/>

                    <TextBlock Grid.Row="8" Grid.Column="0" Text="Language Preference:" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding LanguagePreference, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EditTextBoxStyle}" x:Name="LanguagePreferenceEditTextBox"/>

                    <TextBlock Grid.Row="9" Grid.Column="0" Text="Time Zone:" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding TimeZone, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EditTextBoxStyle}" x:Name="TimeZoneEditTextBox"/>

                    <TextBlock Grid.Row="10" Grid.Column="0" Text="Username (Read-only):" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="10" Grid.Column="1" Text="{Binding Username, Mode=OneWay}" Style="{StaticResource EditTextBoxStyle}" IsReadOnly="True" />

                    <TextBlock Grid.Row="11" Grid.Column="0" Text="Role (Read-only):" Style="{StaticResource EditLabelStyle}"/>
                    <TextBox Grid.Row="11" Grid.Column="1" Text="{Binding Role, Mode=OneWay}" Style="{StaticResource EditTextBoxStyle}" IsReadOnly="True" />

                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,10">
                    <Button Content="Cancel" x:Name="CancelButton" Style="{StaticResource SecondaryButtonStyle}" Click="CancelButton_Click"/>
                    <Button Content="Save Changes" x:Name="SaveChangesButton" Style="{StaticResource PrimaryButtonStyle}" Click="SaveChangesButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </ScrollViewer>
</Page>
